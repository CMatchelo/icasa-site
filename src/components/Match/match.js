const Match = (local, adv, date, hour, resultH, resultA, badge, stadium, champ, round) => {
    return {
      local: local,
      adv: adv,
      date: date,
      hour: hour,
      resultH: resultH,
      resultA: resultA,
      badge: badge,
      stadium: stadium,
      champ: champ,
      round: round
    }
  }

  export default Match;